# Fortify SCA Web Helper

This web application provides a simple interface to search Fortify SCA command features and build shell scripts.

## Setup

```bash
pip install -r requirements.txt
python app.py
```

Open `http://localhost:5000` in your browser.
